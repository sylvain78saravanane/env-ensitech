module.exports = {
    testEnvironment: "node", // Spécifie que nous testons du code Node.js (et non du front-end)
    verbose: true,           // Affiche des détails sur les tests
    collectCoverage: true,   // Génère un rapport de couverture
    coverageDirectory: "coverage", // Où enregistrer la couverture des tests
};

